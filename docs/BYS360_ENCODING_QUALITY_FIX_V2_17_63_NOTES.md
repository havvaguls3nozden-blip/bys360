# BYS360 V2.17.63 Encoding Notları

Bu paket yalnızca encoding kalitesini düzeltir.

- Veritabanına dokunmaz.
- Migration dosyası değiştirmez.
- `.env` veya canlı servis ayarlarını değiştirmez.
- Kullanıcıya yansıyabilecek bozuk Türkçe karakterleri hedefli dosyalarda onarır.
- Python hedeflerinde AST, `py_compile` ve isteğe bağlı `compileall` kontrolü yapar.
